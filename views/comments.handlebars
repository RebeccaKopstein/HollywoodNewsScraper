<div class="container">
    <div class="header clearfix">
        <nav>
            <ul class="nav nav-pills float-right">
                <li class="nav-item">
                    <a class="nav-link active" href="/">Home
                        <span class="sr-only">(current)</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="https://www.eonline.com/news" target="_blank">Go to E!Online</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="https://github.com/rkopstein" target="_blank">Bec's Github</a>
                </li>
            </ul>
        </nav>
        <h3 class="text-muted">Scrape the Latest</h3>
    </div>
    <hr>
    <div class="row">
        <h1>
            <a href="{{result.link}}" target="_blank">{{result.title}}</a>
        </h1>
        <p>{{result.articleSnippet}}</p>
    </div>
    <div class="row">
        <h2>Previous comments</h2>
    </div>

    <div class="row">
        {{#unless result.comment}}
        <p>Nothing to show...yet. Add your opinion(s) below!</p>
        {{/unless}}
    </div>

    <div class="row">
        <ul>
            {{#each result.comment}}
            <li style="list-style: none">
                <h5>Name: {{this.name}} </h5>
                <p>Message: {{this.body}}</p>
                <button class="delete-button btn btn-secondary" data-comment="{{this._id}}">Delete Comment</button>
            </li>
            {{/each}}
        </ul>

    </div>
    <div class="row">
        <h2>Did this article spark any thoughts? If so, comment below!</h2>
    </div>

    <form method="POST" action="/articles/{{result._id}}">
        <div class="form-group row">
            <label for="name" class="col-form-label">Name</label>
            <input class="form-control" type="text" name="name" id="name" required>
        </div>
        <div class="form-group row">
            <label for="body">Message</label>
            <textarea class="form-control" id="body" name="body" rows="3" required></textarea>
        </div>
        <button class="btn btn-primary submit"  type="submit">Submit</button>
    </form>
    <br>
    <br>
    <br>
    <br>
</div>